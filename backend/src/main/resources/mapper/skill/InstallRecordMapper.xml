<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skillsops.skill.mapper.InstallRecordMapper">

    <insert id="upsert">
        INSERT INTO install_record (user_id, skill_id, installed_version)
        VALUES (#{userId}, #{skillId}, #{installedVersion})
        ON DUPLICATE KEY UPDATE
            installed_version = VALUES(installed_version),
            installed_at = CURRENT_TIMESTAMP
    </insert>
</mapper>
