<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skillsops.auth.mapper.UserAccountMapper">

    <resultMap id="UserAccountMap" type="com.skillsops.auth.domain.UserAccount">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="role" property="role"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserAccountMap">
        SELECT id, username, password_hash, role, created_at, updated_at
        FROM user_account
        WHERE username = #{username}
    </select>

    <select id="findById" resultMap="UserAccountMap">
        SELECT id, username, password_hash, role, created_at, updated_at
        FROM user_account
        WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO user_account (username, password_hash, role)
        VALUES (#{username}, #{passwordHash}, #{role})
    </insert>
</mapper>
